import { StyleSheet } from 'react-native';
import { font } from '../../theme';

export const styles = StyleSheet.create({
	textStyle: {
		fontSize: 38,
	},
	titleStyle: {
		fontWeight: 'bold',
	},
	bodyStyle: {
		fontWeight: '200',
	},
});
