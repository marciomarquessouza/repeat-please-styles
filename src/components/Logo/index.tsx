import React from 'react';
import { Image } from 'react-native';
import { logo, logoTitle } from '../../../assets/images';

export const Logo = () => <Image source={logo} />;

export const TitleLogo = () => <Image source={logoTitle} />;
